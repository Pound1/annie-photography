# Set these as environment variables in Netlify (Site settings > Environment
# variables), never commit real values to this repo.

# Shared password for the /admin page (single editor, not a real user system).
ADMIN_PASSWORD=

# From your Cloudinary dashboard (cloudinary.com/console).
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
